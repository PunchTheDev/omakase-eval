.venv/
__pycache__/
*.egg-info/
# runs/ is scratch output, except the published descriptors the dashboard
# renders — those must survive a fresh clone. (`runs/*` not `runs/`: git never
# descends into a fully-ignored directory, so negations under it would be dead.)
runs/*
!runs/benchmarks.json
!runs/showcase.dev.json
